# Exp12 Jetson 性能、功耗、温度与稳定性总结

## 当前状态

`IN_PROGRESS`

Exp12 已完成设备重连审计和事前门槛冻结，尚未产生正式性能、功耗、温度或稳定性结论。
完整计划见 `项目全流程快速学习手册.md`。

## 冻结测试对象

```text
IMX219 sensor-id 0，1920×1080@30
→ Exp11 C++/CUDA/TensorRT FP16 同步端到端链路
→ tegrastats + /proc 1 秒资源采样
```

FP16 Engine SHA256：

```text
88dcc29d2c66b77bdf5b3ac90f327f793516365d5967b51155987a5b736c0a83
```

设备固定为 25W/id 1。正式性能与稳定性运行要求启用 `jetson_clocks`；由于 Jetson
不支持无密码 sudo，启用和最终恢复均由用户在终端输入自己的 sudo 密码完成，凭据不进入
命令、日志或仓库。

## 尚未产生的证据

- 监控与解析工具的 Smoke Test；
- 1,800 帧未锁频基线；
- 三次 1,800 帧固定时钟重复性；
- 54,000 帧、约 30 分钟稳定性结果；
- P50/P95/P99、FPS、RSS、CPU/GPU、功耗、温度、频率和降频结论；
- 时钟恢复后的设备状态。

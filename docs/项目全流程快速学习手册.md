# Jetson PPE 项目全流程快速学习手册

> 这是本项目从立项到收尾的统一学习入口，也是持续追加的实验学习日志。
> 它帮助项目完成后快速理解“为什么做、怎么验证、结果如何、下一步是什么”。
> 精确命令和完整证据仍以各实验总结、日志、JSON/CSV 和 Git 历史为准。

## 1. 使用方法

建议按三遍学习：

1. **10 分钟建立地图**：阅读第 2～7 节，先理解目标、三端分工、主线决策和关键指标；
2. **2 小时理解实验**：阅读第 8 节 Exp00～Exp07，重点理解每个假设为什么接受或拒绝；
3. **面试前复述**：阅读第 9～11 节，用自己的话讲出完整工程链路、指标边界和失败经验。

以后每个实验严格采用两条独立记录：

- 开始前在文末追加“实验前规划”；
- 完成后再追加“实验结果与学习复盘”，不覆盖旧计划。

## 2. 一句话认识项目

本项目在 PPE（人员、头盔、安全背心）小目标检测场景中，以 YOLO11n 为基线，
先用公平消融选择模型，再经过 ONNX、TensorRT、C++ 和 CUDA 完成 Jetson Orin
Nano Super 部署，最后用精度、延迟、吞吐、功耗、温度和稳定性共同验证工程价值。

它不是“训练出一个模型”就结束，而是回答四层问题：

| 层次 | 核心问题 | 主要证据 |
|---|---|---|
| 数据与模型 | 数据可靠吗，模型能检测 PPE 吗？ | 数据审计、P/R、mAP、尺度召回 |
| 结构选择 | 改模型是否真的优于基线？ | 固定划分和配置的消融实验 |
| 部署正确性 | 转换和低精度推理有没有改变结果？ | 张量误差、检测匹配、完整测试集 mAP |
| 系统性能 | Jetson 上是否真正更快、更省、更稳定？ | 端到端 P50/P95/P99、FPS、功耗、温度 |

## 3. 项目主线与当前结论

```text
数据集审计
→ YOLO11n 基线训练
→ P2 / 可重参数化 / 注意力 / Focal 消融
→ 冻结原始 YOLO11n baseline
→ ONNX 导出与一致性验证
→ TensorRT FP32 / FP16
→ INT8 PTQ（下一阶段）
→ TensorRT C++ Runtime
→ CUDA 融合预处理
→ 视频/摄像头端到端推理
→ 性能、功耗、温度与稳定性
→ README、简历与面试材料
```

截至 2026-08-07：

- Exp00～Exp07 已完成；
- Exp03～Exp05 的工程过程有价值，但候选模型均未满足替换基线的综合条件；
- 部署主线仍是 Exp02 原始 YOLO11n baseline；
- Exp06 ONNX 已通过 PyTorch/ONNX Runtime 一致性验证；
- Exp07 TensorRT FP32/FP16 已在 Jetson 通过构建、单图、完整测试集和 GPU-only 诊断验证；
- Exp08 INT8 PTQ 工程链路已完成但候选 REJECT；Exp09 C++ Runtime 已通过逐字节一致性与三进程生命周期验证；下一步为 Exp10 CUDA 融合预处理。

## 4. 三端职责与 Git 串行流程

| 环境 | 主要职责 | 不能替代的工作 |
|---|---|---|
| Windows | 总控、阅读、文档、审查、合并、产物中转 | 不能声称训练或 Jetson 部署已验证 |
| AutoDL RTX 3080 Ti | 数据、训练、评估、PyTorch、ONNX、ORT、量化前准备 | 不能用服务器速度代替板端性能 |
| Jetson Orin Nano Super | TensorRT、CUDA、C++、摄像头、板端性能 | 不负责重新选择训练模型主线 |

推荐的串行 Git 流程：

```text
某一执行端创建实验分支并真实验证
→ push 实验分支
→ Windows 审查 diff、日志和文档
→ 合并 main
→ 另外两端 fetch/pull main
→ 下一执行端从新 main 创建下一实验分支
```

大型模型产物不进入普通 Git。代码和小型记录走 GitHub，`best.pt`、ONNX、Engine、
数据集和完整输出通过 `scp`/`rsync`/Windows 中转，并在发送端和接收端核对 SHA256。
TensorRT Engine 与具体硬件、TensorRT/CUDA 环境绑定，必须在目标 Jetson 重新构建。

## 5. 设备开机后的重连 SOP

AutoDL 是按需租用设备，Jetson 为避免持续发热也会关机。关机是正常状态；每次开机后，
都必须把远程环境当作新的会话检查，不能默认 SSH、后台任务或目录状态仍与上次相同。

用户当前提供的连接入口是：

```powershell
# AutoDL
ssh -p 36913 root@connect.nmb2.seetacloud.com

# Jetson
ssh nvidia@10.198.252.131
```

仓库只记录连接入口，不记录密码、私钥或 Token。端口、IP 或实例变化时，应由用户提供新入口。

每次重连按下面顺序进行：

```text
1. SSH 可达性与免交互认证
2. hostname / whoami / pwd / uname -m，确认没有连错机器
3. 进入约定仓库目录
4. 检查分支、未提交修改、最近提交、远端和领先/落后关系
5. 检查任务所需环境：AutoDL 虚拟环境或 Jetson CUDA/TensorRT
6. 检查输入权重、ONNX、Engine、数据集及 SHA256
7. 判断上次任务是完成、失败保留现场，还是需要重新运行
8. Smoke Test 通过后才进入正式实验
```

最小只读命令：

```bash
hostname
whoami
pwd
uname -m
git rev-parse --show-toplevel
git branch --show-current
git status --short
git log -5 --oneline --decorate
git remote -v
```

若连接失败，要区分网络超时、拒绝连接和认证失败。不要通过反复试错、修改系统 SSH 服务或
重装网络组件来掩盖问题。

## 6. 必须掌握的指标和概念

### 6.1 检测指标

- **Precision**：预测为目标的结果中，有多少是真的；误检越多，Precision 越低。
- **Recall**：真实目标中，有多少被找到；漏检越多，Recall 越低。
- **mAP50**：IoU=0.50 下各类别 AP 的均值，偏向“能否大致检测到”。
- **mAP50-95**：IoU 0.50～0.95 多阈值平均，更严格地评价定位与分类综合质量。
- **tiny/small recall**：直接检查业务关注的小目标是否被找回，不能被整体 mAP 掩盖。
- **固定阈值 P/R** 与 **AP** 不是同一指标：前者依赖一个置信度阈值，后者来自整条 PR 曲线。

### 6.2 模型转换

- **PyTorch**：训练和研究阶段的动态图模型；
- **ONNX**：跨框架中间表示，本项目冻结为静态 batch 1、640×640、FP32、外置 NMS；
- **TensorRT Engine**：为目标 GPU 和软件栈优化后的执行计划，不能跨设备直接复用；
- **FP32 / FP16 / INT8**：数值位宽逐步降低，通常更快更小，但必须验证精度损失；
- **PTQ**：训练后量化，不重新训练，通过代表性校准数据确定 INT8 量化尺度。

### 6.3 性能计时边界

GPU-only 只回答 Engine 内核执行速度，不等于用户看到的一帧总延迟。端到端通常包括：

```text
图像采集/解码 → CPU/GPU 预处理 → H2D → TensorRT → D2H → 后处理/NMS → 显示/输出
```

因此最终必须同时报告平均值、P50、P95、P99、FPS，并写清 warmup、迭代次数、Batch、
输入尺寸、功耗模式、`jetson_clocks`、是否包含预处理和数据传输。

## 7. 冻结输入、关键产物与性能参考

### 7.1 基线模型

| 项目 | 冻结值 |
|---|---:|
| Precision | 0.92161767 |
| Recall | 0.82040743 |
| mAP50 | 0.89270104 |
| mAP50-95 | 0.52047856 |
| tiny+small recall | 0.79020979 |
| 参数量 | 2,590,425 |

`best.pt` 来源：

```text
/root/autodl-tmp/jetson-ppe-outputs/
exp02_6_yolo11n_baseline_e100_20260804_185444/weights/best.pt
```

SHA256：

```text
79dad73ccad09d46299083078f6d7e19c38541bc19ac86a8d3f11e49661d6ae6
```

### 7.2 Exp06 ONNX

```text
输入       [1, 3, 640, 640]
输出       [1, 7, 8400]
精度       FP32
opset      17
dynamic    false
simplify   false
NMS        外置
文件大小   10,566,605 bytes
SHA256     305e23c65aa3b1d01e7b1a784c355665228f435f0b904b92ed2618954736d1f8
```

### 7.3 Exp07 Jetson Engine

Engine 仅保存在 Jetson，不进入 Git：

| 精度 | SHA256 | 大小 | GPU-only mean |
|---|---|---:|---:|
| FP32 | `01616a8144228db5edbf8948227e3bbaee43b22c495aba3c6c44212e43efe0f1` | 14,880,428 bytes | 10.241488 ms |
| FP16 | `88dcc29d2c66b77bdf5b3ac90f327f793516365d5967b51155987a5b736c0a83` | 8,951,540 bytes | 3.479713 ms |

FP16 相对 FP32 的 GPU-only 加速约 2.943 倍，Engine 大小约降低 39.84%。该结果关闭
H2D/D2H，使用 batch 1、640×640、500 ms warmup、200 次迭代、CUDA Graph 和 spin wait，
且未锁定 `jetson_clocks`，不能当作最终端到端结论。

Jetson 同运行时完整测试集 mAP50-95：

| 后端 | mAP50-95 |
|---|---:|
| PyTorch FP32 | 0.52189519 |
| TensorRT FP32 | 0.52160406 |
| TensorRT FP16 | 0.52192881 |

## 8. Exp00～Exp07 学习复盘

### Exp00：项目初始化与范围冻结 — PASS

**核心问题**：如何让后续每次实验都可复现、可追溯且不覆盖历史？

建立仓库、Git/GitHub SSH、忽略规则、README、ROADMAP、项目范围、实验索引、模板和注册表。
最重要的成果不是某个指标，而是建立了“实验编号—分支—配置—日志—总结—决策”的证据链。

必须能讲清楚：大型模型不进普通 Git；失败实验也保留；在真实设备验证前不能写成 PASS。

### Exp01：Jetson 环境与编译链审计 — PASS

**核心问题**：板端的软件栈和 C++/CUDA/TensorRT 工具链是否真的可用？

确认 Jetson Orin Nano Super、aarch64、L4T R36.4.3、CUDA 12.6.68、cuDNN 9.3、
TensorRT 10.3.0.30、GCC/G++ 11.4、CMake 3.22.1、OpenCV 4.10、GStreamer 1.20.3。
CUDA 向量加法最大绝对误差为 0，TensorRT Runtime、OpenCV 和 GStreamer smoke test 通过。

关键失败经验：把全部代码放进 `.cu` 会触发 NVCC 与 GLib 宏冲突，最小修复是将普通主程序
交给 `g++`，CUDA kernel 单独由 `nvcc` 编译。另一个经验是 `trtexec --version` 在没有模型时
可能返回失败，不能只凭一个返回码判断 TensorRT 不可用。

### Exp02：数据审计与 YOLO11n 基线 — PASS

**核心问题**：数据集能否支持公平实验，原始 YOLO11n 的可靠基准是什么？

数据划分为 train 980 张、val 217 张/834 个目标、test 219 张/840 个目标，类别是 person、
helmet、safety_vest。审计发现并清理了一个重复标注。先做 1 epoch smoke test，再进行固定
seed=42、100 epoch、batch=16、AdamW 的正式训练，耗时约 717 秒。

独立 test 结果是 P=0.92161767、R=0.82040743、mAP50=0.89270104、
mAP50-95=0.52047856。固定阈值审计得到 GT=840、TP=731、FP=169、FN=109；
tiny recall=24/29=0.827586，small recall=89/114=0.780702，合计 0.790210。

关键失败经验：Shell heredoc、Python 标准输入和 `tee` 组合曾造成阻塞；预训练权重缺失也导致
smoke test 失败。正式长实验前的小测试能把这些工程问题提前暴露。

### Exp03：P2 小目标检测头消融 — REJECT

**实验假设**：加入 stride=4 的高分辨率 P2 检测头，可能提高 tiny/small 召回。

工程链路运行成功，但 test mAP50-95 从 0.52047856 降到 0.51273409，mAP50 从
0.89270104 降到 0.88405588；tiny recall 从 0.82759 降到 0.62069，small recall 从
0.78070 降到 0.75439。GFLOPs 从 6.4 增至 10.4（约 +62.5%），训练时间约 +16.1%，
RTX 推理约慢 30.4%，batch=16 的一次审计还触发 OOM。

结论：更高分辨率特征并不自动等于更好的小目标检测。数据规模、预训练适配、优化难度和计算压力
都会改变结果。因为业务关注的小目标更差且成本更高，所以拒绝替换基线。

### Exp04：部署可重参数化结构消融 — REJECT

**实验假设**：训练时使用多分支增强表达，部署前融合为单个 3×3 卷积，可能兼顾精度和速度。

分支融合在数值和检测指标层面几乎等价，证明“训练结构 → 部署结构”的转换工程是正确的。
候选模型 test mAP50-95 提升约 0.00275683，整体 Recall 也略升，但 tiny TP 从 24 降到 16，
tiny recall 从 0.82759 降到 0.55172。把阈值降到 0.001 也只恢复到 19 个 tiny TP，说明问题
不只是置信度阈值。

结论：重参数化转换 PASS，不代表候选模型可以进入业务主线。必须把“转换数值等价”“总体 AP”
和“关键尺度目标”拆开验收，因此最终 REJECT，但保留为重要的部署工程学习成果。

### Exp05：轻量注意力与 Focal 损失消融 — REJECT

**实验假设**：P3 残差式 CBAM-Lite 或 Focal 分类损失可能提升难样本和小目标表现。

注意力只增加 4,259 个参数（约 +0.1644%），mAP50-95 仅增加 0.00008167，
tiny+small recall 却下降 0.034965。Focal 的 mAP50-95 增加 0.006459，但 mAP50 下降
0.012815，tiny+small recall 下降 0.083916。两个单变量都没有通过门槛，因此按预设停止规则
跳过 attention+focal 组合，避免无效算力消耗。

结论：总体 AP 小幅提升不能代替业务关键尺度指标；停止规则必须在看结果之前设定。

### Exp06：PyTorch → ONNX 与一致性验证 — PASS

**核心问题**：冻结基线能否转换成 TensorRT 可接收的 ONNX，并保持数值和检测语义一致？

导出静态 FP32 ONNX：batch=1、640×640、opset 17、dynamic=false、simplify=false、外置 NMS。
模型通过 ONNX checker 和 ONNX Runtime 前向。单图原始输出 max abs error=0.00061035、
mean abs error≈1.03e-5、relative L2≈1.27e-7；30 个检测的数量和类别完全一致。
完整测试集在相同 `rect=false`、batch=1 条件下，PyTorch mAP50-95=0.51376814，
ORT=0.51385152，差值 +0.00008338。

关键经验：导出成功不等于部署正确；至少要验证模型结构、原始张量、NMS 后结果和完整数据集 AP。
曾遇到 NMS 导入错误、Shell 语法、平均误差阈值过严、`rect` 配置不一致，以及错误地用同一个
置信度阈值计算 P/R 和 AP，这些都通过缩小失败点后修复。

### Exp07：Jetson TensorRT FP32 / FP16 — PASS

**核心问题**：ONNX 能否在目标 Jetson 构建并运行，FP16 是否在保持检测精度时获得收益？

FP32 和 FP16 Engine 均在 TensorRT 10.3 的目标 Jetson 本机构建，完成单图、完整测试集和
GPU-only 诊断 benchmark。FP16 约为 FP32 的 2.943 倍，Engine 小约 39.84%；同运行时
完整测试集 mAP50-95 与 PyTorch/FP32 参考基本一致，因此 FP16 成为后续运行时主选，FP32
继续作为正确性参考。

关键失败经验包括：SSH 输出管道提前关闭导致 SIGPIPE；FP16 原始误差阈值过严；自动依赖安装
长时间停滞；不同 Ultralytics 版本的 AP 口径不可直接比较；在同一进程顺序加载多个 Engine 引发
CUDA context 清理错误。最终使用同一个 Jetson 验证器、各后端独立进程、公平固定预处理和
后处理完成验证。这里的 GPU-only 数值仍不是端到端性能。

## 9. 当前可复述的项目讲解

### 60 秒版本

我做的是一个 Jetson Orin Nano Super 上的 PPE 小目标检测与推理优化项目。项目先审计数据，
训练并冻结 YOLO11n 基线，再对 P2 检测头、可重参数化、轻量注意力和 Focal loss 做固定数据划分
的单变量消融。部分方案总体 AP 略有改善，但 tiny/small 召回变差或计算成本显著增加，所以没有
为了“有创新点”强行替换基线。之后我把冻结模型导出为静态 ONNX，通过原始张量、NMS 后检测和
完整测试集 mAP 验证 PyTorch/ORT 一致性，再在 Jetson 本机构建 TensorRT FP32/FP16 Engine。
FP16 在 GPU-only 计时中约加速 2.94 倍且大小降低约 39.84%，完整测试集精度基本保持。INT8 PTQ
又获得约 25.44% GPU-only 延迟下降和约 39.82% Engine 缩小，但 mAP50-95 与 tiny+small recall
超过预冻结退化门槛，因此没有为了速度牺牲关键尺度检测，部署主线保留 FP16。之后用 TensorRT
10.3 C++ API 实现 Runtime，三独立进程的原始输出都与 Python 参考逐字节一致。下一步继续 CUDA
预处理和包含功耗温度的端到端测试。

### 必须避免的夸大

- 不能说 P2、注意力或 Focal 提升了最终模型，它们的最终决策是 REJECT；
- 不能把 3.4797 ms 直接说成摄像头实时端到端延迟；
- 不能说 INT8 已被部署主线采用，也不能说 CUDA 融合、功耗温度测试已经完成；
- 不能用 RTX 3080 Ti 的速度代表 Jetson；
- 不能把 Engine 构建成功等同于精度验证成功。

## 10. 高频面试问题

### 为什么最终仍使用原始 YOLO11n？

因为替换条件是综合验收，不是只看一个总体指标。P2 使关键指标和成本都变差；重参数化候选虽有
轻微 mAP50-95 增益，但 tiny recall 大幅下降；注意力和 Focal 也损害 tiny+small recall。
原始基线在当前数据和业务目标下更稳健。

### 为什么 ONNX 要做四层验证？

checker 只证明图结构合法，前向成功只证明能运行；原始张量比较检查数值，NMS 后比较检查检测
语义，完整测试集 AP 才检查误差是否会积累成业务影响，四层关注点不同。

### 为什么 FP16 更快但还需要 FP32？

FP16 利用 Jetson 的低精度硬件路径，通常更快更小；FP32 数值误差更小，是定位转换或量化问题的
正确性参考。只有验证 FP16 的最终检测和数据集指标后，才能把它作为部署主选。

### 为什么 INT8 需要校准集？

激活从浮点映射到 8 位整数需要合理的数值范围。校准集要代表真实训练分布，并复用完全相同的
预处理；不能使用测试集，否则会造成评估泄漏。校准数量不是越多越好，应通过预先设计的消融冻结。

## 11. 项目剩余路线

| 实验 | 状态 | 主要学习目标 |
|---|---|---|
| Exp08 INT8 PTQ | REJECT | 校准、量化误差、精度—速度折中 |
| Exp09 TensorRT C++ Runtime | PASS | Engine 生命周期、绑定、内存和异步执行 |
| Exp10 CUDA 融合预处理 | PLANNED | letterbox/颜色/归一化/布局融合与参考对比 |
| Exp11 视频/摄像头 | PLANNED | GStreamer、缓冲队列和端到端数据流 |
| Exp12 系统测试 | PLANNED | 延迟分位数、功耗、温度、稳定性和降频 |
| Exp13 项目收尾 | PLANNED | README、复现实验、简历和面试材料 |

## 12. 后续追加模板

```markdown
## YYYY-MM-DD — ExpXX 实验前规划

- 状态：PLANNED
- 核心问题：
- 实验假设：
- 执行机器与三端分工：
- 冻结输入、Commit 和 SHA256：
- 控制变量：
- Smoke Test：
- 正式步骤：
- 验收阈值：
- 风险与停止条件：
- 本实验需要学会：

## YYYY-MM-DD — ExpXX 实验结果与学习复盘

- 状态：PASS / FAIL / REJECT / SKIPPED / BLOCKED
- 环境、Commit、命令和输出目录：
- 真实指标和产物 SHA256：
- 失败、定位与最小修复：
- 已证实：
- 尚未证实：
- 最终决策：
- 必须能讲清楚的原理：
- 面试问答：
- 下一步：
```

---

<!-- 后续实验从此处继续追加；不要覆盖或重写上方历史记录。 -->

## 2026-08-07 — Exp08 INT8 PTQ 实验前规划

- **状态**：PLANNED；尚未构建 INT8 Engine，尚无 INT8 精度或性能结果。
- **核心问题**：在 Jetson Orin Nano Super 上，INT8 PTQ 能否相对 FP16 进一步减小 Engine、
  降低推理时间，同时把完整测试集和 tiny/small 精度损失控制在预设范围内？
- **实验假设**：具有代表性的训练集校准样本、与 Exp06/Exp07 完全一致的预处理，以及目标 Jetson
  本机校准和构建，可以获得可接受的精度—性能折中；是否接受必须由真实结果决定。
- **执行机器与分工**：Windows 负责计划、审查和 Git；AutoDL 只从训练集审计并生成可复现的
  校准清单/样本，不接触 test 作为校准输入；Jetson 负责 TensorRT INT8 校准、Engine 构建、
  正确性、完整测试集和 GPU-only 诊断验证。
- **冻结输入**：Exp02 `best.pt` SHA256 为
  `79dad73ccad09d46299083078f6d7e19c38541bc19ac86a8d3f11e49661d6ae6`；Exp06 ONNX
  SHA256 为 `305e23c65aa3b1d01e7b1a784c355665228f435f0b904b92ed2618954736d1f8`；
  Exp07 FP32/FP16 结果作为比较参考。
- **先决审计**：重新连接三端；检查 Git 和输入哈希；确认 TensorRT 10.3 的 INT8 构建接口、
  校准缓存行为和支持算子；统计训练集类别、尺度和场景分布后，才冻结校准样本数量与抽样种子。
- **控制变量**：batch=1、640×640、相同 letterbox、BGR→RGB、归一化、HWC→CHW、NMS、
  test 划分、置信度/IoU、计时方式、功耗模式和 Engine 构建环境。测试集只用于最终评估。
- **Smoke Test**：使用小型训练集校准子集构建一个 INT8 Engine；单张固定图片执行一次；检查
  构建返回码、输出形状 `[1,7,8400]`、有限值、NMS 后检测以及与 FP32/FP16 参考的差异。
- **正式实验**：冻结代表性校准清单和 SHA256；构建时间戳 Engine；记录构建命令、TensorRT
  版本、校准缓存、大小和哈希；完成单图、完整 test、尺度召回和同口径 GPU-only benchmark。
- **验收原则**：AP 使用严格且事先冻结的退化门槛，固定阈值 P/R 和 tiny/small 使用单独门槛；
  只有正确性、精度和性能全部有真实证据时才能 PASS。具体数值阈值在读取 Exp07 验收配置与
  校准审计后冻结，并在运行正式实验前追加记录，不能看完 INT8 结果后再放宽。
- **风险**：校准数据不代表真实分布；预处理一处不一致导致量化尺度错误；test 泄漏；TensorRT
  API/校准缓存与版本绑定；某些层回退到高精度；只看构建成功或平均延迟而误判完成。
- **停止条件**：Smoke Test 不通过、哈希不一致、校准集来源不清、输出出现 NaN/Inf、或三端 Git
  未同步时，不进入正式实验；失败目录和日志保留。
- **本实验需要学会**：PTQ 原理、校准集代表性、per-tensor/per-channel 尺度概念、量化误差、
  混合精度回退、校准缓存、数据泄漏，以及如何用精度—速度—大小三维证据做部署决策。

## 2026-08-07 — 学习手册建立记录

- 首次汇总 Exp00～Exp07 的问题、做法、真实结果、失败经验和最终决策；
- 建立设备重启后的 SSH 重连 SOP；
- 从 Exp08 起执行“实验前先追加计划、实验后再追加复盘”的不可回写流程；
- 本记录只整理已存在证据，没有把 Exp08 或后续实验表述为完成。

## 2026-08-07 — Exp08 计划冻结补充（正式 INT8 结果产生前）

- **状态变更**：Exp08 从 `PLANNED` 进入 `IN_PROGRESS`；当前仍没有 INT8 Engine 或结果。
- **重连审计**：Windows、AutoDL、Jetson 已同步到 `main@1eed7cc`；AutoDL 训练集 980 张；
  Jetson 上 ONNX、FP32、FP16 哈希与冻结记录一致，TensorRT 10.3 报告支持快速 INT8。
- **校准集冻结**：只从 train 选择 256 张，基础 seed=42；比较 1024 个确定性随机候选，
  选择类别、尺寸、类别×尺寸和背景比例最大偏差最小者。偏差必须 `<=0.02`，覆盖全部类别、
  tiny/small/medium/large 和背景图。val/test 不得进入校准。
- **精度门槛**：相对同运行时 FP16，test mAP50-95 下降不超过 0.010，mAP50 下降不超过
  0.015；固定 `conf=0.25`、`NMS IoU=0.70`、匹配 IoU=0.50 的 tiny+small recall
  下降不超过 0.050。
- **性能门槛**：同 Exp07 GPU-only 口径下，mean latency 至少比 FP16 低 5%，Engine
  至少比 FP16 小 10%。这仍不是端到端性能。
- **决策规则**：工程构建、输出有限值、精度、关键尺度召回、延迟和大小均通过，INT8 才能替换
  FP16；任何强制门槛失败都保留结果并拒绝候选，不在看完结果后放宽门槛。

## 2026-08-07 — Exp08.0 校准集准备结果与学习复盘

- **状态**：校准数据阶段 `PASS`；Exp08 总体仍为 `IN_PROGRESS`，尚未构建 INT8 Engine。
- **真实运行**：AutoDL 分支 `exp/08-int8-calibration`，正式 run 为
  `exp08_0_prepare_calibration_20260807_145356`。
- **输入与隔离**：完整 train 为 980 张/3,922 个实例；仅使用 train，未使用 val/test。
- **选择结果**：256 张/996 个实例，基础 seed=42，在 1,024 个候选中选择 seed=639；
  类别为 person=401、helmet=306、safety_vest=289，尺寸为 tiny=28、small=139、
  medium=270、large=559，并包含 2 张背景图。
- **代表性**：类别、尺寸、类别×尺寸和背景比例的最大绝对偏差为 0.0053134517，低于预设
  0.02 门槛。
- **归档**：32,384,828 bytes，SHA256 为
  `a3056d1e1852bc55f10455a32e493cfcf4ebcaaad6558d1811a90b20e21bed72`；
  manifest SHA256 为 `75b0c94f49aafc133402a43793dea40a7ca76131959b04043c87e69b44bd6d1d`。
- **负向 smoke**：最初 16 张小样本虽能生成归档，但没有覆盖背景图和 tiny，代表性检查返回
  FAIL。没有放宽门槛，改用正式冻结配置后通过。学习点是“能校准”不等于“校准数据有代表性”。
- **尚未证实**：该校准集能否让 INT8 保持精度和获得性能收益，必须在 Jetson 构建后验证。
- **下一步**：传输归档并双端校验哈希；Jetson 从合并后的 main 新建 INT8 分支做 smoke build。

## 2026-08-07 — Exp08 INT8 PTQ 实验结果与学习复盘

- **状态**：工程链路完成，候选 `REJECT`；部署运行时继续使用 Exp07 FP16 Engine。
- **三端协作**：AutoDL 从 train-only 数据生成 256 张校准集；Windows 中转归档并审核 Git；
  Jetson TensorRT 10.3 在本机校准、构建、评估和计时。归档三端 SHA256 一致。
- **正式 Engine**：5,387,244 bytes，SHA256 为
  `5787fb3bae4dbd00909c1762efc9263566044bc4dc35a836c950312e85895f26`；输出
  `[1,7,8400]` 且无 NaN/Inf，说明构建与执行链路 `PASS`。
- **完整 test AP**：FP16/INT8 mAP50-95 分别为 0.52192881/0.50801483，INT8 下降
  0.01391398，超过 0.010 门槛；mAP50 下降 0.00632768，通过 0.015 门槛。
- **关键尺度**：固定阈值下 FP16/INT8 tiny+small recall 分别为 0.79020979/0.48951049，
  下降 0.30069930，远超允许的 0.050。tiny TP 从 24 降至 7，small TP 从 89 降至 63。
- **性能收益**：同次 GPU-only 诊断中 FP16/INT8 mean 为 3.640914/2.714804 ms；INT8
  加速 1.3411×、延迟降低 25.4362%、Engine 缩小 39.8177%，性能和尺寸门槛均通过。
- **失败与修复**：首次长构建因 SSH 输出通道关闭触发 BrokenPipe，构建产物本身仍存在；改为
  远端完整运行和精简终端输出。首次尺度审计把 219 张图一次传给静态 batch=1 Engine，修复为
  串行执行。两个失败目录均保留，没有删除或改写成 PASS。
- **核心原理**：PTQ 的标签分布代表性是必要条件，但不是充分条件。量化误差由各层激活动态范围、
  离群值和敏感层共同决定；因此 Engine 能运行、总体 mAP50 尚可、速度明显提升，都不能替代
  mAP50-95 与业务关键尺度召回验收。
- **面试回答**：INT8 并非必然优于 FP16。本实验说明必须用预先冻结的精度—性能多目标门槛决策；
  当速度和尺寸通过而 tiny/small 召回严重退化时，应拒绝候选并保留证据，而不是事后放宽门槛。
- **尚未证实**：QAT、敏感层回退或其他校准策略能否恢复关键尺度精度；若研究，应另立新实验。
- **下一步**：Exp09 以已验证 FP16 Engine 为输入，实现 TensorRT C++ Runtime 的加载、绑定、
  显存管理、异步执行、输出读取和与 Python/参考后端的一致性验证。

## 2026-08-07 — Exp09 TensorRT C++ Runtime 实验前规划

- **状态**：`IN_PROGRESS`；当前只冻结方案，尚无 C++ 编译或运行结果。
- **核心问题**：能否脱离 Python/Ultralytics，用 TensorRT 10.3 C++ API 正确、安全、可重复地
  执行 Exp07 冻结 FP16 Engine，并得到与 Python TensorRT 相同的原始输出和检测语义。
- **范围隔离**：输入使用 Python 已完成预处理的连续 FP32 NCHW binary；C++ 只负责 Engine
  反序列化、I/O 检查、显存、Stream、异步复制、`enqueueV3`、同步和原始输出。图像预处理属于
  Exp10，C++ NMS、视频与摄像头属于后续实验。
- **冻结 Engine**：SHA256 为
  `88dcc29d2c66b77bdf5b3ac90f327f793516365d5967b51155987a5b736c0a83`，输入
  `[1,3,640,640]`，输出 `[1,7,8400]`。
- **正确性门槛**：输出有限且形状完全匹配；相对 Python TensorRT 的 raw max abs `<=0.001`、
  mean abs `<=1e-5`、relative L2 `<=1e-5`；同一 Python NMS 下检测数与类别一致，box 最大误差
  `<=0.1 px`、confidence 最大误差 `<=0.001`。
- **生命周期门槛**：smoke 为 warmup 2 + 5 次；正式为 warmup 20 + 200 次；至少 3 个独立进程
  返回 0 且输出 SHA256 一致。必须使用 RAII 清理 Runtime、Engine、Context、Stream 和 Device Buffer。
- **计时边界**：同时记录 host wall 与 CUDA Event 分布，明确 H2D/D2H 范围；不与 Exp07
  `--noDataTransfers` 的 GPU-only 数值混称，延迟不是本实验唯一 PASS 条件。
- **停止条件**：Engine/输入哈希错误、I/O 模式/类型/形状错误、CUDA/TensorRT 调用失败、输出
  NaN/Inf、smoke 正确性或重复进程失败时，保留现场并停止正式实验，不事后放宽门槛。
- **本实验要学会**：TensorRT 10 tensor API、Engine/Context 生命周期、host/device 内存、CUDA
  Stream 与同步、异步错误传播、原始输出一致性，以及怎样让 Runtime 与预处理/后处理解耦。

## 2026-08-07 — Exp09 TensorRT C++ Runtime 实验结果与学习复盘

- **状态**：`PASS`；下一实验为 Exp10 CUDA 融合预处理。
- **真实运行**：Jetson 分支 `exp/09-trt-cpp-runtime`；smoke 为
  `exp09_0_cpp_runtime_smoke_20260807_164202`，正式 run 为
  `exp09_1_cpp_runtime_formal_20260807_164714`。
- **实现结构**：C++17 + CMake；Runtime 类通过 RAII 管理 TensorRT Runtime/Engine/Context、
  CUDA Stream/Event 和输入输出 Device Buffer，使用 TensorRT 10 的 tensor API 与 `enqueueV3`。
- **固定输入**：FP16 Engine SHA256 为 `88dcc29…c0a83`；预处理输入 binary 为 4,915,200 bytes，
  SHA256 为 `963a6747…fe90f`；输出固定为 FP32 `[1,7,8400]`、235,200 bytes。
- **正确性**：C++ 输出 SHA256 `29ae405e…62c5d`，与 Python TensorRT 参考逐字节相同；raw
  max/mean/relative-L2 均为 0，NMS 后 30 个检测的类别、框和置信度误差均为 0。
- **生命周期**：3 个独立进程，每个 warmup 20 + 200 次，全部返回 0，三份输出哈希一致。
- **诊断计时**：三个进程 host mean 为 6.606200～6.638301 ms；进程 1 的 P50/P95/P99 为
  6.612337/8.364161/9.915610 ms。范围包含 pageable H2D、enqueue、D2H 和同步，不等于 Exp07
  关闭传输的 3.479713 ms，也不等于摄像头端到端延迟。
- **核心原理**：Engine 的 FP16 指内部优化精度，I/O 仍可声明为 FP32。必须按 Engine 元数据计算
  元素数和字节数，在 `enqueueV3` 前绑定所有 tensor 地址，并在读取 host 输出前完成 Stream 同步。
- **为什么要逐字节比较**：只看 30 个检测一致可能漏掉低置信度候选差异；原始张量一致证明 C++
  生命周期、地址、大小和同步没有改变 Engine 结果，NMS 一致再证明检测语义保持。
- **边界**：Exp09 已证明预处理后 Tensor 的 C++ 推理正确；没有证明 C++/CUDA 图像预处理、NMS、
  摄像头、功耗温度或长期稳定性。这些必须在 Exp10～Exp12 单独验收。
- **面试回答**：把 Python 推理迁移到 C++ 时，关键不是“能调用 Engine”，而是 Engine 哈希、I/O
  元数据、内存字节数、异步生命周期、错误传播、原始输出和多进程重复性都有可审计证据。
- **下一步**：Exp10 以当前固定输入 binary 为黄金参考，分别比较 CPU Reference 与 CUDA 融合
  letterbox/颜色转换/归一化/布局输出，再把 CUDA 输出直接连接本 C++ Runtime。

## 2026-08-07 — Exp10 CUDA 融合预处理实验前规划

- **状态**：`IN_PROGRESS`；当前只冻结设计，尚无 CUDA 编译、正确性或性能结果。
- **核心问题**：能否用一个 CUDA Kernel 保持 Exp07 OpenCV letterbox 语义，同时融合 padding、
  BGR→RGB、归一化和 HWC→CHW，并让输出直接留在 device 供 Exp09 Runtime 使用。
- **测试输入**：现有图像统一为 640×640，不能覆盖 letterbox。由冻结探针确定性生成 640×640、
  360×640、640×360、720×1280、480×240 五个 PNG 夹具，覆盖无缩放、横纵 padding、放大和缩小。
- **CPU Reference**：`cv::resize(INTER_LINEAR)` + `copyMakeBorder(114)` + RGB + `/255` + NCHW；
  resize 尺寸和四边 padding 必须完全一致。
- **正确性门槛**：输出 `[1,3,640,640]` 且有限；max abs `<=2/255+1e-7`、mean abs
  `<=0.0005`、relative L2 `<=0.001`、P99 abs `<=1/255+1e-7`；padding 必须精确。
- **性能门槛**：正式使用 `hd_wide`，warmup 20 + 200 次；CUDA kernel-only mean 至少比 CPU
  mean 低 30%。pageable H2D+kernel+D2H 另行报告但不作强制门槛，因为部署时输出不应回传 host。
- **计时边界**：不包含 JPEG 解码；CPU、kernel-only、total 分开记录 mean/P50/P95/P99；未锁定
  `jetson_clocks` 时只作诊断，不能提前声称摄像头端到端收益。
- **停止条件**：Smoke 的 `hd_wide` resize/padding、颜色、布局、有限值或误差失败时不进入正式测试；
  正式任一夹具或性能门槛失败时保留结果，不事后放宽。
- **本实验要学会**：half-pixel 双线性插值、边界与舍入、二维线程映射、coalesced NCHW 写入、
  CUDA Event 计时、传输与 Kernel 计时边界，以及 device-to-device 流水线为何避免 D2H。

## 2026-08-07 — Exp10 CUDA 融合预处理结果与学习复盘

- **状态**：`PASS`；5 种输入形状的 CUDA 输出均与 Jetson OpenCV 4.10 CPU Reference
  逐元素一致，下一实验为 Exp11 视频/摄像头端到端推理。
- **正式运行**：Jetson 分支 `exp/10-cuda-preprocess`，最终 run 为
  `exp10_1_cuda_preprocess_formal_20260807_173638`，结果对应 Commit `e2ee8c2`。
- **融合内容**：一个二维 CUDA Kernel 完成 half-pixel letterbox resize、114 padding、BGR→RGB、
  uint8→FP32 `/255` 和 HWC→NCHW；输出固定为 `[1,3,640,640]` 连续 device tensor。
- **正确性**：square、wide、tall、hd_wide、small_tall 的 max/mean/relative-L2/P99/padding
  误差全部为 0；所有几何、形状和有限值检查通过。
- **性能**：`hd_wide`、warmup 20 + 200 次时，CPU/kernel-only/pageable total mean 为
  2.282124/0.200761/1.883069 ms；kernel-only 比 CPU 下降 91.2029%，通过预冻结 30% 门槛。
- **为什么 total 不是核心门槛**：total 把输入传到 GPU，又把 4.9 MB FP32 输出传回 CPU；实际
  TensorRT 流水线应让预处理输出留在 GPU，所以 kernel-only 才反映即将连接的 device 流程。
- **关键失败 1**：第一次 CMake 在非登录 SSH 环境找不到 CUDA compiler；显式使用
  `/usr/local/cuda/bin/nvcc` 修复，没有重装或修改系统 CUDA。
- **关键失败 2**：朴素双线性在 4/3 放大时只有 1 灰度级误差，但 relative L2 超过门槛；普通
  11-bit 合并定点和“半整数统一向下”两种修复假设也失败，所有目录均保留。
- **真正根因**：OpenCV 4.10 的 CV_8U `INTER_LINEAR` 专用纵向路径会在两阶段定点运算之间截断
  中间位。要逐像素一致，必须复现运算顺序和中间截断，不能只实现数学上等价的双线性公式。
- **工程经验**：优化代码的黄金参考不仅包括几何公式，还包括数据类型、量化系数、边界、舍入、
  中间精度和平台 SIMD 路径；误差很小也必须按事先门槛决策。
- **尚未证实**：本实验没有把 device tensor 直接绑定 TensorRT，没有验证摄像头、NMS、端到端
  延迟、功耗、温度或稳定性；这些属于 Exp11～Exp12。
- **面试回答**：CUDA 预处理为什么需要 CPU Reference？因为颜色、letterbox、布局或 1 灰度级
  舍入差异都会改变模型输入。应先逐元素证明语义等价，再分开报告 kernel 与传输时间，最后才接入
  TensorRT 测端到端收益。
- **下一步**：Exp11 在 Jetson 串行连接 GStreamer/视频帧、CUDA 预处理、Exp09 FP16 C++ Runtime
  和检测后处理；预先冻结队列、丢帧、同步、延迟与摄像头不可用时的文件视频替代规则。

## 2026-08-07 — Exp11 视频/摄像头端到端推理实验前规划

- **状态**：`IN_PROGRESS`；本记录只冻结设计与门槛，尚无 Exp11 编译或运行结果。
- **核心问题与假设**：能否在 Jetson 上用同一个 C++ 程序串行连接 GStreamer 视频帧、Exp10 CUDA
  融合预处理、Exp09 FP16 TensorRT Runtime、YOLO11 三类别后处理与 NMS，并在文件视频和真实
  IMX219 两种输入上保持有限输出、有效检测语义和可审计的逐帧结果。
- **执行机器与三端分工**：Jetson 负责编译、文件视频和 IMX219 实测；Windows 负责方案冻结、diff
  审查、Git 合并和文档；AutoDL 已由用户关机，本实验默认不需要训练、导出或新的服务器产物，
  因而不连接服务器。若后续发现确需 AutoDL，必须先提醒用户开机并等待确认。
- **冻结输入**：FP16 Engine 位于 Jetson Exp07 归档目录，大小 8,951,540 bytes，SHA256 为
  `88dcc29d2c66b77bdf5b3ac90f327f793516365d5967b51155987a5b736c0a83`；固定输入/输出形状为
  `[1,3,640,640]`/`[1,7,8400]`。文件基线为 `/home/nvidia/imx219_test_sensor0.mp4`，元数据为
  H.264 High、1920×1080、59/3 FPS、时长 7.609333 秒；真实源为 sensor-id 0 的 IMX219。
- **控制变量**：FP16 主线不变；batch=1、imgsz=640、padding=114、BGR→RGB、FP32 `/255`、
  NCHW、`conf=0.25`、class-aware NMS `IoU=0.70`；同一 CUDA Stream 上依次执行 H2D、融合
  kernel、`enqueueV3` 和输出 D2H。文件输入禁用丢帧以保证确定性；摄像头 `appsink` 固定
  `max-buffers=1 drop=true sync=false`，优先处理最新帧，避免处理延迟无限积累。
- **实施步骤与 Smoke Test**：先把 CUDA 预处理改为可复用的持久化 device buffer，并允许其输出
  直接绑定 TensorRT 输入；再实现 C++ 解码/采集、YOLO11 解码、坐标反 letterbox、分类别 NMS、
  逐帧 CSV 和汇总 JSON。文件 smoke 只处理 5 帧，要求打开成功、恰好处理 5 帧、输出全有限、
  检测框/类别/置信度合法；通过后才运行完整文件，并连续启动 3 个独立进程验证逐帧检测摘要一致。
  最后运行 IMX219 30 帧 smoke，再运行 300 帧功能验收。
- **验收阈值**：文件正式运行必须处理完整视频且无读取/推理失败，3 个独立进程返回码均为 0、
  处理帧数相同、逐帧检测摘要 SHA256 一致；每帧 raw 输出无 NaN/Inf，类别只允许 0～2，置信度
  位于 `[0.25,1]`，映射回原图后的框在图像边界内且面积为正。IMX219 smoke 和正式运行必须成功
  建链；正式运行恰好处理 300 帧且采集、CUDA、TensorRT、NMS 均无失败。端到端 mean/P50/P95/
  P99、阶段耗时和有效 FPS 必须真实报告，但 Exp11 不以某个 FPS 或延迟数值判 PASS；性能、功耗、
  温度和长期稳定性的强制门槛属于 Exp12。
- **风险与停止条件**：Engine 哈希变化、GStreamer 建链失败、帧格式/尺寸异常、device 地址或 Stream
  生命周期错误、CUDA/TensorRT 返回失败、NaN/Inf、非法检测或文件 smoke 不确定时，保留时间戳
  失败目录并停止后续阶段，不事后放宽门槛。若真实摄像头因硬件/Argus 外部状态不可用，文件视频
  结果只能标为文件链路 PASS，不能替代摄像头 PASS；应将摄像头部分明确记为 `BLOCKED` 或 `FAIL`。
- **本实验预期学会**：GStreamer source/caps/appsink、实时流背压与丢帧策略、CUDA Stream 上的
  producer-consumer 顺序、TensorRT 外部 device tensor 绑定、YOLO11 `[1,7,8400]` 解码、
  letterbox 坐标逆变换、class-aware NMS，以及功能验收与性能/稳定性实验边界。

## 2026-08-07 — Exp11 视频/摄像头端到端推理结果与学习复盘

- **状态**：`PASS`；文件视频和真实 IMX219 均完成 GStreamer→CUDA→FP16 TensorRT→NMS
  端到端功能验收，下一实验为 Exp12 性能、功耗、温度与稳定性测试。
- **真实环境与 Commit**：Jetson Orin Nano Super、25W 模式、CUDA 12.6.68、TensorRT 10.3、
  OpenCV 4.10、GStreamer 1.20；结果对应 `exp/11-video-camera@1325268`。AutoDL 全程关机，
  因为本实验不需要新的训练或导出产物。
- **冻结产物**：FP16 Engine SHA256 `88dcc29…c0a83`；C++ 程序 359,856 bytes、SHA256
  `bf3717d8…a4a5c0`；文件视频 874,518 bytes、SHA256 `f00e116a…a66665`。
- **文件正式运行**：三个独立进程各处理完整 150 帧并产生 151 个检测；逐帧检测 CSV SHA256
  均为 `9f3f3345…e34e2d8a`。第一次端到端 mean/P50/P95/P99 为
  15.695/16.491/17.168/17.603 ms，有效离线吞吐 63.713 FPS；`sync=false`，不能将该吞吐
  误写成视频原始播放帧率。
- **摄像头正式运行**：Argus 使用 sensor-id 0、1920×1080@30；Smoke 30/30 帧通过，正式
  300/300 帧、300 个检测、返回 0。端到端 mean/P50/P95/P99 为
  31.832/31.958/34.190/34.760 ms，有效处理率 31.415 FPS；检测 CSV SHA256 为
  `07219d40…971860`。
- **阶段耗时**：摄像头正式运行的取帧/CUDA 预处理/TensorRT+输出 D2H/CPU 后处理 mean 分别为
  15.500/1.995/11.552/0.447 ms。当前为可审计串行基线，并在阶段计时点同步；不能与 Exp07
  关闭传输、CUDA Graph、spin wait 的 3.480 ms GPU-only 结果直接比较。
- **真实失败与最小修复**：首个文件 Smoke 在 OpenCV 自动 GStreamer 管线的 `qtdemux` 处失败；
  使用 fakesink 单独证明显式硬件解码链可用后，将程序改为明确的 qtdemux、h264parse、
  nvv4l2decoder、颜色转换和 appsink。失败目录 `exp11_0_file_video_smoke_20260807_210320`
  保留，没有删除或改写为 PASS。
- **回归证据**：公共 API 改动后，Exp09 输出仍与 Python TensorRT 参考逐字节相同，SHA256
  `29ae405e…62c5d`；Exp10 CPU/CUDA 输出仍逐元素相同，max abs=0。
- **已证实**：CUDA 融合输出可直接作为 TensorRT device 输入；文件流能确定性处理全部帧；
  实时流容量 1 的队列可以避免无界积压；YOLO11 解码、反 letterbox、分类别 NMS、结果合法性
  检查和首末帧可视化均工作。人工检查首末帧人员框在原图内且语义合理。
- **尚未证实**：摄像头多场景/三类别精度、NVMM 零拷贝、异步重叠、GPU NMS、锁频公平性能、
  CPU/GPU/内存占用、功耗、温度、降频、丢帧率和长时间稳定性，全部留给 Exp12。
- **必须讲清楚的原理**：文件源为了复现使用 `drop=false`；实时源为了低时延使用单帧队列并
  `drop=true`。所谓 device 直连，是预处理结果不回 host 且直接绑定 TensorRT 输入地址；
  但原始摄像头 BGR 帧仍做 pageable H2D，TensorRT 输出仍做 D2H，不能称为全链路零拷贝。
- **面试回答**：为什么不只报 FPS？因为功能正确、离线吞吐、摄像头节拍、阶段同步、尾延迟、
  功耗温度和长期降频是不同问题。Exp11 证明链路能正确工作，Exp12 才在固定功耗与时钟条件下
  用 P50/P95/P99、资源、功耗、温度和稳定性形成最终板端性能结论。

## 2026-08-08 — Exp12 Jetson 性能、功耗、温度与稳定性实验前规划

- **状态**：`IN_PROGRESS`；当前只完成重连审计和门槛冻结，尚无 Exp12 性能或稳定性结果。
- **核心问题与假设**：Exp11 的同步 C++ 端到端链路能否在 Jetson 25W 模式下稳定跟上
  IMX219 1920×1080@30 输入，并在固定时钟后保持可重复的尾延迟、可控温度/功耗和无明显
  内存增长的 30 分钟连续运行；`jetson_clocks` 预计降低频率伸缩造成的抖动，但不预设一定加速。
- **执行机器与三端分工**：Jetson 执行摄像头、锁频、tegrastats、进程资源与稳定性实测；Windows
  冻结门槛、审查 diff、合并文档；AutoDL 保持关机，Exp12 不需要新模型或服务器产物。Windows
  和 AutoDL 的速度、功耗或温度不得代替 Jetson 结论。
- **冻结输入与代码**：继续使用 FP16 Engine，SHA256
  `88dcc29d2c66b77bdf5b3ac90f327f793516365d5967b51155987a5b736c0a83`；Exp11 程序
  SHA256 `bf3717d8b4feb17617ea4e831dcc6fffdb69a13d52449c0727aa8697ada4a5c0`；
  sensor-id 0、1920×1080@30、batch=1、imgsz=640、`conf=0.25`、class-aware NMS IoU 0.70、
  `appsink max-buffers=1 drop=true sync=false`。若监控功能需要改代码，必须重新记录 Commit 与哈希。
- **设备基线**：`nvpmodel` 固定为 25W/id 1，不改变系统 CUDA、TensorRT、驱动或 JetPack；
  初始 CPU governor 为 `schedutil`、GPU governor 为 `nvhost_podgov`，CPU policy max 1.344 GHz、
  GPU max 918 MHz。锁频需要用户在 Jetson 终端输入自己的 sudo 密码；Codex 不读取或保存密码。
  正式实验结束后必须执行 `sudo jetson_clocks --restore` 恢复原始动态调频并再次核查。
- **计时与监控范围**：应用端端到端包含取帧、pageable H2D、CUDA 融合预处理、TensorRT、
  原始输出 D2H 和 CPU NMS；warmup 不进入正式 CSV。每 1 秒记录 tegrastats 的 RAM/SWAP、各 CPU
  利用率与频率、GR3D、各温区、VDD_IN/VDD_CPU_GPU_CV/VDD_SOC，并从 `/proc/<pid>` 记录 RSS、
  线程数和进程 CPU。必须报告 mean/P50/P95/P99/min/max，不能只报告 FPS。
- **分阶段步骤**：先开发独立监控/解析脚本；未锁频 Smoke 处理 300 帧并验证采样、解析、应用
  返回码和输出完整性。随后做一次 1,800 帧未锁频基线。用户手动启用 `jetson_clocks` 并核查后，
  做 3 个独立的 1,800 帧固定时钟性能 run；三次通过后才运行 54,000 帧（约 30 分钟）稳定性测试。
  最后恢复动态调频。所有阶段均使用时间戳目录，失败日志保留。
- **Smoke Test 门槛**：应用恰好处理 300 帧、返回 0、无 NaN/Inf/非法框/采集早停；至少获得
  5 条有效 tegrastats 和 5 条进程样本；summary/frames/detections/资源 CSV/环境/命令/返回码均存在。
  任一失败则停止正式实验。
- **固定时钟短测门槛**：每次恰好处理 1,800 帧、返回 0；端到端 P95 `<=40 ms`、P99
  `<=50 ms`、有效处理率 `>=29.0 FPS`。三次 mean 延迟和 FPS 的变异系数分别 `<=5%`，三次
  P95 的最大相对跨度 `<=10%`。这些阈值基于 Exp11 300 帧 P95 34.190 ms/31.415 FPS 预先留裕量，
  结果产生后不得放宽。
- **30 分钟稳定性门槛**：恰好处理 54,000 帧并返回 0；全程端到端 P95 `<=40 ms`、P99
  `<=50 ms`、有效处理率 `>=29.0 FPS`；最后 10% 帧的 P95 相对最初 10% 退化 `<=10%`；
  RSS 末值相对首值增长 `<=64 MiB` 且线性增长斜率 `<=1 MiB/min`；SWAP 使用保持 0；CPU/GPU/Tj
  最高温度必须 `<70°C`，即不触及已审计到的 CPU/GPU 70°C passive trip。VDD_IN mean/P95/max
  和 CPU/GPU 利用率必须报告，但不设置事后臆造的功耗收益门槛。
- **频率与丢帧解释**：固定时钟前后都记录 governor、current/max frequency 和
  `jetson_clocks --show`。OpenCV 接口没有可靠暴露 Argus PTS/上游 drop counter，因此本实验以
  固定帧数、总 wall time、有效 FPS、采集失败数和 `appsink` 单帧队列策略评价实时跟随能力；
  不得把 `30×wall_time-frame_count` 写成精确硬件丢帧数，只能作为估计或限制说明。
- **风险和停止条件**：Engine/程序哈希错误、25W 模式变化、锁频状态不明、监控解析失败、应用
  非零退出、摄像头/Argus/CUDA/TensorRT 错误、温度达到 90°C 安全停止线、SSH 中断后远端进程
  状态不明或磁盘不足时，保存现场并停止后续阶段。不得隐藏失败、删除目录或放宽冻结门槛。
- **本实验预期学会**：DVFS 与锁频、公平 benchmark、warmup/测量边界、吞吐与摄像头节拍、
  P95/P99 尾延迟、tegrastats 各字段、功耗轨、thermal trip/降频、RSS 泄漏趋势、短时重复性与
  长稳态的区别，以及为什么“平均 30 FPS”不能证明设备长期可靠。
